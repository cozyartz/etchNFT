---
import BaseLayout from '../layouts/BaseLayout.astro';
import OrderHistory from '../components/react/OrderHistory';

const client: 'only' = 'only';
---

<BaseLayout title="My Orders – EtchNFT">
  <section class="hero-gradient min-h-screen px-6 py-12">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-12">
        <img src="/etchlogo.png" alt="EtchNFT Logo" class="w-12 h-12 mx-auto mb-4 animate-neon-pulse" />
        <h1 class="text-4xl sm:text-5xl font-cyber font-bold mb-6 text-white glitch" data-text="MY ORDERS">
          <span class="text-neon-pink animate-neon-pulse">MY</span> <span class="text-secondary animate-flicker">ORDERS</span>
        </h1>
        <p class="text-xl text-gray-300 mb-8">
          Track your etched NFTs and view your complete order history.
        </p>
      </div>

      <OrderHistory client:only={client} />
    </div>
  </section>
</BaseLayout>